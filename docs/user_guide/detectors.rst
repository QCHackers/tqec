Automatic detector computation
==============================

The automatic detector computation has moved to a standalone package called ``tqecd``.

The corresponding documentation can be found
`here <https://tqec.github.io/tqecd/>`_.
